document.addEventListener('DOMContentLoaded', function() {
    console.log("Página de Bienvenido cargada.");

    // Ejemplo: Hacer que los botones de navegación realmente naveguen si no se usa onclick en HTML
    // const navButtons = document.querySelectorAll('.nav-button');
    // navButtons.forEach(button => {
    //     button.addEventListener('click', function() {
    //         const pageName = this.textContent.toLowerCase().trim();
    //         if (pageName === 'fichaje') {
    //             window.location.href = 'fichaje.html';
    //         } else if (pageName === 'bienvenid@') {
    //             // ya estás aquí
    //             // window.location.href = 'bienvenido.html';
    //         }
    //         // ... añadir más casos para otros botones
    //     });
    // });

    // Aquí podrías añadir lógica específica para la página de bienvenida si es necesario
    // Por ejemplo, si el video es un archivo local y quieres controlarlo con JS,
    // o si quieres cargar la descripción dinámicamente.
});